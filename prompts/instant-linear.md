# Instant Linear Execution Profile

Use `instant` for exact-file, unambiguous tasks that should go straight to a direct worker.

Expected flow: read only the specified files, make the minimal change, run requested or narrow validation when available, and return compact evidence.

Do not scout broadly, redesign, or expand scope beyond the named files and task.
